#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @m: the integer to be computed
 * Return: the absolute value
 */
int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	return (-m);
}
